fastapi>=0.110
uvicorn[standard]>=0.29
# Required by FastAPI to parse form-encoded bodies (Twilio WhatsApp webhooks).
python-multipart>=0.0.9
pydantic>=2.6
httpx>=0.27
asyncpg>=0.29
aiosqlite>=0.19
redis>=5.0
# Production checkpointing (optional). The supervisor falls back to its own
# Postgres-backed checkpointer if these aren't installed.
# langgraph>=0.2
# langgraph-checkpoint-postgres>=2.0
# Hermes channel drivers (optional — install only the channels you deploy).
# The gateway/governance core and inbound Twilio signature validation have no
# third-party dependency; these are only needed to run the live channels:
#   Slack (Socket Mode):
# slack-bolt>=1.20
#   WhatsApp (Twilio outbound REST client):
# twilio>=9.0
# FastAPI/uvicorn (required above) host the WhatsApp + Web routers.
# Test deps
pytest>=8.0
pytest-asyncio>=0.23
flake8>=6.0
