fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
asyncpg>=0.29
aiosqlite>=0.19
redis>=5.0
# Production checkpointing (optional). The supervisor falls back to its own
# Postgres-backed checkpointer if these aren't installed.
# langgraph>=0.2
# langgraph-checkpoint-postgres>=2.0
# Test deps
pytest>=8.0
pytest-asyncio>=0.23
flake8>=6.0
